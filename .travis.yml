language: node_js
node_js:
  - 14
script:
  - eslint 'src/**'
  - npm run test:ci
before_deploy:
  - rvm $(travis_internal_ruby) --fuzzy do ruby -S gem install faraday -v 1.10.0
deploy:
  provider: heroku
  api_key: $HEROKU_API
  app: clean-architecture-react
  skip_cleanup: true 